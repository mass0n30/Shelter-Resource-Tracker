
// wrapper and toggle between resources, notes, timeline components for client profile page, also includes client information and resource summary ? (currently figuring this out for this component)

export default function MainInfoContainer() {

  return (
    <div className="mainInfoContainer">

    </div>
  );
}

// toggles to show Resource information (assigned/last updated, follow up, notes)
// toggle has buttons to update status (pending, accepted, ect)
function Resources() {

  return (
    <div className="resources">
      
    </div>
  );
}

// just shows note, red if priority, and estimate date
function Notes() {

  return (
    <div className="notes">
      
    </div>
  );
}

// need library for this one, and figure out timeline sentence format
function Timeline() {

  return (
    <div className="timeline">
      
    </div>
  );
}